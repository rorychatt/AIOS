[package]
name = "aios-daemon"
version = "0.1.0"
edition = "2021"

[dependencies]
aios-core = { path = "../aios-core" }
serde_yaml = "0.9"
serde_json = "1.0"
reqwest = { version = "0.11", features = ["json", "blocking"] }
